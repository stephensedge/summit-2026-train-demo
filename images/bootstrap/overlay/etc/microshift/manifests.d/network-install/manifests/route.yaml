---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: pubsrv
spec:
  to:
    kind: Service
    name: pubsrv
  port:
    targetPort: http
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: resources
spec:
  to:
    kind: Service
    name: install-resources
  port:
    targetPort: http